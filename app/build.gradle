apply plugin: 'com.android.application'

android {
    compileSdkVersion 33
    defaultConfig {
        applicationId "com.example.android"
        minSdkVersion 21
        targetSdkVersion 33
        versionCode 1
        versionName "1.0"
        namespace "com.example.android"  // Thêm namespace ở đây
    }
}

dependencies {
    implementation "androidx.appcompat:appcompat:1.6.1"
}
